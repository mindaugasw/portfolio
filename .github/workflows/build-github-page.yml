name: Build GitHub page

on:
  push:
    # TODO remove test branch
    branches: [ "master", "pages-test" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build the Docker image
        run: docker compose -f builder/docker-compose.yml up --build
